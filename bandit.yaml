# Bandit configuration for KaliGhost Pro
# Professional security scanning

exclude_dirs:
  - '/test/'
  - '/venv/'
  - '/build/'
  - '/dist/'

skips:
  - 'B101'  # Skip assert_used checks in tests

tests: []